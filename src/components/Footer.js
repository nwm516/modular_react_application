import React from "react";

const Footer = () => {
    return (
      <footer>
        <p>&copy; {new Date().getFullYear()} Bottom of the application. No where else to go from here.</p>
      </footer>
    );
  };
  
  export default Footer;